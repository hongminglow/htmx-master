<% if (enabled) { %>
  <div class="info-strip">Trusted devices keep the session available for longer.</div>
<% } else { %>
  <div class="field-feedback muted">Session ends on normal browser expiration.</div>
<% } %>
