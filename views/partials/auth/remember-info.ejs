<% if (enabled) { %>
  <div class="info-strip">The server would issue a longer session cookie for trusted devices.</div>
<% } else { %>
  <div class="field-feedback muted">Session ends on normal browser expiration.</div>
<% } %>
